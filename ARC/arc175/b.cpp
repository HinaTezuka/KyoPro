#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

void print_element(int x) { // std::for_each(vec.begin(), vec.end(), print_element);
    std::cout << x << " ";
}

int main() {

    return 0;
}